{
  "_HINWEIS": "Kopiere diese Datei nach appsettings.Production.json. Alles hier ist optional — fehlende Geheimnisse erzeugt der Server beim ersten Start selbst und legt sie in appsettings.Local.json ab.",

  "Https": {
    "_HINWEIS": "Zertifikat aus dem Windows-Zertifikatspeicher (z. B. per AD CS ausgestellt). Alternativ CertificatePath auf eine PFX-Datei zeigen lassen.",
    "CertificateThumbprint": "AB12CD34EF56...",
    "RedirectToHttps": true
  },
  "ClientApi": {
    "_HINWEIS": "Windows = Kerberos/NTLM, ohne Schlüsselverteilung. ApiKey nur, wenn die Clients nicht in der Domäne sind.",
    "Authentication": "Windows"
  }
}
